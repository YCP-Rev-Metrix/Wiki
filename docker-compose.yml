version: '3'

services:
  wiki:
    build:
      context: .
      dockerfile: Dockerfile
    image: revmetrixproject/wiki
    container_name: wiki
    ports:
      - "1313:1313"
    networks:
      - revmetrix_net

networks:
  revmetrix_net:
    driver: bridge